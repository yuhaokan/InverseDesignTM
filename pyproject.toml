[project]
name = "inverse-design"
version = "0.1.0"
description = "Inverse design of transmission matrices using RL and MEEP"
requires-python = ">=3.11"

[tool.setuptools.packages.find]
where = ["src"]

[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"
